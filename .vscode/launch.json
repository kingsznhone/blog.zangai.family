{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Run npm server",
            "runtimeExecutable": "npm",
            "runtimeArgs": [
                "run",
                "server"
            ],
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}/hexo",
            "preLaunchTask": "Clean and Generate"
        }
    ]
}